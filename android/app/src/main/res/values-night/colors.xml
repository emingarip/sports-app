<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Matches the dark background the Flutter UI paints, so the handover
         from splash to first frame does not flash. -->
    <color name="launch_background">#121212</color>
</resources>
